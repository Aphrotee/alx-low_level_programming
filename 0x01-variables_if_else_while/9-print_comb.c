#include <stdio.h>

/**
 * main - entry point
 *
 * Return: 0 Success
 */
int main(void)
{
	char num;

	num = '0';
	while (num < '9')
	{
		putchar(num);
		putchar(com);
		putchar(' ');
		num++;
	}
	putchar('9');
	return (0);
}
