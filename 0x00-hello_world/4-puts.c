#include <stdio.h>
#include <string.h>

/**
 * Entry point - main
 * The character type variable str is declared
 * The string value is assigned to it
 * puts function is used to print out the string in str
 * Return (0)
 */
int main(void)
{
	char str[]="Programming is like building a multilingual puzzle\n";
	
	puts(str);
	
	return (0);
}	
