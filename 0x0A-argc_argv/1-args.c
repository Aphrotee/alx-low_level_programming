#include <stdio.h>

/**
 * main - entry point
 * @argc: size of command array
 * @argv: command array
 *
 * Return: 0
 */
int main(int argc, char *argv[])
{
	(void)argv;
	printf("%d\n", argc);
	return (0);
}
